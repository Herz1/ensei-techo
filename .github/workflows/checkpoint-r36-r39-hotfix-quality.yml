name: Round 36-39 Hotfix Quality Checkpoint

on:
  push:
    branches:
      - checkpoint/2026-09-15-r36-r39-hotfix-quality
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: npm run build
